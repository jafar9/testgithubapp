name: Jekyll site CI

on: 
   push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: create deployment
      run: |
        echo "this buiid is done"
